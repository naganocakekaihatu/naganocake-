<%= form_with model: order, url:admin_order_path(order), method: :patch, local: false do |f| %>
  <%= f.select :status, Order.statuses.keys.map {|k| [I18n.t("enums.order.status.#{k}"),k]} %>
  <%= f.submit "更新", class: "btn btn-success" %>
<% end %>